{:contract/version 1
 :contract/namespace game.liveops
 :features #{:remote-config :feature-flags :segments :scheduled-windows
             :deterministic-rollout :variants :subject-overrides
             :campaigns :optimistic-revisions}
 :allocation {:buckets 10000 :stable true}
 :limits {:config-bytes 65536 :conditions 50 :variants 20 :campaign-grants 100}
 :host-responsibilities #{:authentication :authorization :persistence :clock
                          :audit-log :cache-control :telemetry}}
