{:contract/id :kotoba.game/cloud-save :contract/version 1 :namespace game.cloud-save
 :revision {:immutable true :head :optimistic :history-max 50 :checksum :host-verified}
 :conflict {:merge :three-way :resolution :explicit :paths :vectors}
 :rollback {:creates-new-revision true :rewrites-history false}
 :lifecycle #{:snapshot :history :commit :merge :rollback :export :erase}
 :host-responsibilities #{:identity-proof :encryption :durable-persistence :size-limit
                          :checksum :clock :retention :data-portability}}
