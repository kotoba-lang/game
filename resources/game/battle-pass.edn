{:contract/id :kotoba.game/battle-pass
 :contract/version 1
 :runtime :cljc
 :season {:clock :host :lifecycle #{:scheduled :active :closed}}
 :xp {:verified true :idempotency :xp/event-id}
 :tracks #{:free :premium}
 :premium {:source :verified-entitlement}
 :claim {:idempotent true :host-grant-atomic true}}

